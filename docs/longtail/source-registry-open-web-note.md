# Radar source-registry note

The persistent source-family registry is complemented by [`open-web-discovery.md`](./open-web-discovery.md).

Known sources answer **what changed at places we already know**. Open-web discovery answers **what opportunity ecosystems exist that Radar has never seen before**. Search hits remain `DISCOVERY_ONLY` until an official source is hydrated and verified.
